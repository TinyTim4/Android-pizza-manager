package com.example.project5main;

/**
 * User created custom class (all classes not specified as such are required)
 * Deep Dish Brooklyn
 * Pan Thin
 * Stuffed Hand tossed
 * allow the store staff to choose the pizza style, flavors, and sizes, but not the crusts
 * @author Tim Liu, Eric Hou
 */
public enum Crust {
    DEEP_DISH,
    PAN,
    STUFFED,
    THIN,
    HAND_TOSSED,
    BROOKLYN
}